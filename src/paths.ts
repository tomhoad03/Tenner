const Paths = {
  Root: '/',
  Launchpad: '/',
  Services: '/services',
  ServiceCategory: '/services/:category',
  Listing: '/services/listing/:serviceID',
  Profile: '/profile/:uid',
  Orders: '/orders/:uid',
  Settings: '/settings/profile'
}

export default Paths
